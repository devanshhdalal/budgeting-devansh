/** Standard card image dimensions (credit-card aspect). */
export const CARD_IMAGE_WIDTH = 200;
export const CARD_IMAGE_HEIGHT = 126;
export const CARD_IMAGE_ASPECT = `${CARD_IMAGE_WIDTH} / ${CARD_IMAGE_HEIGHT}`;
